#!/usr/bin/env -S sh -eu
SOURCE=$1
TARGET=$2
TIMESTAMP=$3
mkdir -p $TARGET/archive/all/$TIMESTAMP $TARGET/latest
ln -s ../archive/all/20240207T1742CET $TARGET/latest/all
if [ ! -z "$(ls $SOURCE)" ]; then
    cp -a $SOURCE/* $TARGET/archive/all/$TIMESTAMP
fi
